<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyOlimp"
        tools:targetApi="31">
        <activity
            android:name="ramble.sokol.myolimp.feature_onborading.activities.OnBoardingActivity"
            android:exported="false" />
        <activity
            android:name="ramble.sokol.myolimp.feature_onborading.activities.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ramble.sokol.myolimp.feature_onborading.activities.MainActivity"
            android:exported="false" />
    </application>

</manifest>