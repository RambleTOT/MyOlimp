<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyOlimp"
        tools:targetApi="31">
        <activity
            android:name=".ui.presentation.MainActivity"
            android:exported="false" />
        <activity
            android:name=".feature_authentication.presentation.activities.MainAuthenticationActivity"
            android:exported="false" />
        <activity
            android:name=".feature_splash_onBoarding.presentation.activities.OnBoardingActivity"
            android:exported="false"
            android:noHistory="true" />
        <activity
            android:name=".feature_splash_onBoarding.presentation.activities.SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>